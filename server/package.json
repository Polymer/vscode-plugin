{
  "name": "polymer-vscode-server",
  "description": "Polymer language server for vscode.",
  "version": "0.0.1",
  "author": "Polymer Project Authors",
  "license": "MIT",
  "engines": {
    "node": "*"
  },
  "repository": {
		"type": "git",
		"url": "https://github.com/Polymer/vscode-plugin"
	},
  "dependencies": {
    "@types/node": "^6.0.31",
    "polymer-analyzer": "^2.0.0-alpha.8",
    "vscode-languageserver": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^2.0.0"
  },
  "scripts": {
    "compile": "installServerIntoExtension ../client ./package.json ./tsconfig.json && tsc -p .",
    "watch": "installServerIntoExtension ../client ./package.json ./tsconfig.json && tsc --watch -p ."
  }
}
